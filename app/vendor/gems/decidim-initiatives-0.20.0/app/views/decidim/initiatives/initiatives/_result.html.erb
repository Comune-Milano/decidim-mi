<% if initiative.answered? && translated_attribute(initiative.answer).present? %>
  <br>
  <div class="row column">
    <div class="callout success">
      <h5><%= t(initiative.state, scope: "decidim.initiatives.initiatives.result.answer_title") %>:</h5>
      <p>
        <% if initiative.answer_url.present? %>
          <a href="<%= initiative.answer_url %>" target="_blank">
            <%= decidim_sanitize translated_attribute initiative.answer %>
          </a>
        <% else %>
          <%= decidim_sanitize translated_attribute initiative.answer %>
        <% end %>
      </p>
    </div>
  </div>
<% end %>

<% if initiative.rejected? %>
  <br>
  <div class="row column">
    <div class="callout alert">
      <p>La petizione non ha raggiunto il numero necessario di firme. Non può quindi ricevere risposta dall’Amministrazione.
        <a href="https://partecipazione.comune.milano.it/pages/petizioni">Maggiori informazioni</a>
      </p>
    </div>
  </div>
<% end %>

<% if initiative.accepted? %>
  <br>
  <div class="row column">
    <div class="callout success">
      <p>La petizione ha raggiunto il numero necessario di firme ed è in attesa della risposta dell’Amministrazione.
      L’esito della petizione viene notificato agli organi competenti e pubblicato sull’Albo Pretorio e sulla piattaforma Milano Partecipa.
        <a href="https://partecipazione.comune.milano.it/pages/petizioni">Maggiori informazioni</a></p>
    </div>
  </div>
<% end %>

<% if initiative.published? && initiative.signature_end_date < Date.current %>
  <br>
  <div class="row column">
    <div class="callout warning">
      <p>E’ in corso il conteggio delle firme cartacee</p>
    </div>
  </div>
<% end %>
