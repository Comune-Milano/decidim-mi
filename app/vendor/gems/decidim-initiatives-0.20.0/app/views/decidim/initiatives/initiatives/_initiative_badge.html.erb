<% if initiative.rejected? || initiative.accepted? || initiative.published? || initiative.discarded? || initiative.validating? %>
  <span class="<%= state_badge_css_class(initiative) %> label initiative-status">
    <%= humanize_state_partecipata initiative %>
  </span>
<% end %>
